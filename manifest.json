{
  "name": "ludflow",
  "version": "0.5.19",
  "download_url": "https://github.com/DeeJanuz/ludflow-mcpviews/releases/download/v0.5.19/ludflow-plugin.zip",
  "description": "Ludflow MCP Views plugin for code intelligence and knowledge management",
  "frame_origins": [
    "https://app.ludflow.com",
    "https://staging.app.ludflow.com",
    "http://localhost:3000",
    "http://127.0.0.1:3000"
  ],
  "registry_index": {
    "summary": "Code intelligence, data governance, and knowledge management tools for the Ludflow platform",
    "tags": [
      "code-analysis",
      "search",
      "data-governance",
      "knowledge-management"
    ],
    "plugin_rules": [
      "When working on a Decidr decision or project and discovery produces findings worth preserving (architecture analysis, design specs, prerequisite research), propose creating a Ludflow document using write_document. Only propose documents when there is a relevant Decidr entity to attach to \u2014 do not create documents speculatively.",
      "If discovery reveals a prerequisite to the current decision, evaluate whether it warrants a new Decidr decision: create a new decision if the prerequisite touches a different module/scope OR affects other projects/decisions beyond the current one. Otherwise, bundle it into the current decision. When creating a new prerequisite decision, create a Ludflow document capturing the rationale and link it.",
      "When creating or updating a Ludflow document, proactively search Decidr for related entities scoped to the current initiative. Propose links via link_document \u2014 never auto-link without user confirmation. If matches exist in other initiatives, mention them but do not auto-link across initiatives.",
      "When creating documents, search the Knowledge Dex (get_business_concepts) for relevant business concepts and use consistent terminology. When discovering unfamiliar domain terms during work, propose adding them via manage_knowledge_entries."
    ],
    "tool_groups": [
      {
        "name": "Search",
        "hint": "Full-text and vector search across codebase, docs, and data governance",
        "tools": [
          "search_codebase",
          "vector_search"
        ]
      },
      {
        "name": "Code Analysis",
        "hint": "Inspect code units, modules, dependencies, and file contents",
        "tools": [
          "get_code_units",
          "get_module_overview",
          "get_dependencies",
          "get_file_content",
          "get_analysis_stats"
        ]
      },
      {
        "name": "Data Governance",
        "hint": "Explore, create, and manage data sources, tables, metadata drafts, columns, and data lake",
        "tools": [
          "get_data_schema",
          "get_data_lake_schema",
          "get_column_context",
          "manage_data_sources",
          "manage_data_tables",
          "manage_data_draft"
        ]
      },
      {
        "name": "Knowledge & Docs",
        "hint": "Manage business concepts, knowledge entries, and documents",
        "tools": [
          "get_business_concepts",
          "manage_knowledge_entries",
          "get_document",
          "write_document",
          "move_document",
          "delete_document",
          "list_documents"
        ]
      },
      {
        "name": "Organization",
        "hint": "Discover organizations, MCPViews auth status, and embedded app sessions",
        "tools": [
          "list_organizations",
          "create_app_embed_session"
        ]
      }
    ],
    "renderer_names": [
      "ludflow_app",
      "ludflow_documents_home",
      "ludflow_data_governance",
      "ludflow_knowledge_dex"
    ]
  },
  "plugin_rules": [
    "Ludflow is a multi-org OAuth plugin. Before working in a different organization, call list_organizations to discover available orgs and token status. For follow-up Ludflow tool calls in that org, include organization_id.",
    "If list_organizations shows mcpviews_token_status is missing or expired_unrefreshable for the target org, call start_plugin_auth with plugin_name 'ludflow' and that organization_id before retrying. MCPViews uses in-app email-code auth by default; use auth_flow 'browser' only for advanced OAuth recovery."
  ],
  "tool_rules": {
    "list_organizations": "Use to discover which organizations the user belongs to and whether MCPViews already has an org-scoped token. Results are enriched with has_mcpviews_token, mcpviews_token_status, and mcpviews_token_refreshable. When switching to another org, include organization_id in subsequent Ludflow tool calls. If the target org token is missing or expired_unrefreshable, call start_plugin_auth with that organization_id; expired_refreshable should be treated as connected while MCPViews refreshes it.",
    "create_app_embed_session": "Internal renderer bridge tool. Use only when opening the real Ludflow app inside MCPViews. It returns a one-time embed_url and must not be shown or stored as a reusable login token.",
    "manage_data_sources": "Use to list, create, update, delete, and inspect raw-source content for data governance data sources. Creating/updating requires org admin or owner permissions; deleting requires owner permissions. Passing DDL/CSV content on create imports tables immediately. Use MCPViews review before destructive delete actions.",
    "manage_data_tables": "Use to list tables for a data source, import new DDL/CSV tables into an existing source, rename tables, delete tables, and inspect delete impact. Import/rename/delete require org admin or owner permissions. Use action=impact before delete and MCPViews review before destructive delete actions."
  },
  "no_auto_push": [
    "create_app_embed_session"
  ],
  "renderers": {
    "search_codebase": "rich_content",
    "vector_search": "rich_content",
    "get_code_units": "rich_content",
    "get_document": "rich_content",
    "write_document": "rich_content",
    "move_document": "rich_content",
    "delete_document": "rich_content",
    "list_documents": "rich_content",
    "get_data_schema": "structured_data",
    "manage_data_sources": "structured_data",
    "manage_data_tables": "structured_data",
    "manage_data_draft": "structured_data",
    "get_data_lake_schema": "structured_data",
    "get_dependencies": "rich_content",
    "get_file_content": "rich_content",
    "get_module_overview": "rich_content",
    "get_analysis_stats": "rich_content",
    "get_business_concepts": "structured_data",
    "manage_knowledge_entries": "structured_data",
    "get_column_context": "rich_content",
    "list_organizations": "rich_content",
    "create_app_embed_session": "rich_content"
  },
  "renderer_definitions": [
    {
      "name": "rich_content",
      "description": "Markdown display for Ludflow organization discovery and auth status",
      "scope": "tool",
      "tools": [
        "list_organizations"
      ],
      "rule": "Transform list_organizations output into rich_content markdown. Title: 'Ludflow Organizations'. Body: For each org show the name, slug, role, whether it is the current token's org, and mcpviews_token_status. Treat expired_refreshable as connected/refreshable. If mcpviews_token_status is missing or expired_unrefreshable, tell the user to authenticate that org before switching."
    },
    {
      "name": "rich_content",
      "description": "Markdown display with plugin citations for search results",
      "scope": "tool",
      "tools": [
        "search_codebase",
        "vector_search"
      ],
      "rule": "Transform search results into rich_content markdown. If an 'answer' field is present, use it as the body with inline citations. Otherwise, title: 'Search Results (N)'. Body: Group results by type. For each result, show name, brief description, and a plugin citation link [n](cite:plugin:ludflow:TYPE:ID) so users can click to see full detail in a slideout. Include citation objects in data.citations.plugin array."
    },
    {
      "name": "rich_content",
      "description": "Markdown display with plugin citations for code units",
      "scope": "tool",
      "tools": [
        "get_code_units"
      ],
      "rule": "Transform code units into rich_content markdown. Title: 'Code Units'. Body: For each unit, create a heading with the name, badge the unit_type, show file_path:line_start-line_end. Include source in a fenced code block with the appropriate language. Add plugin citations: [n](cite:plugin:ludflow:code_unit:unitId) for each unit so users can click to see full detail in a slideout."
    },
    {
      "name": "structured_data",
      "description": "Tabular data with hierarchical rows, change tracking, sort/filter, and review mode",
      "scope": "tool",
      "tools": [
        "get_data_schema",
        "get_data_lake_schema",
        "manage_data_sources",
        "manage_data_tables"
      ],
      "data_hint": "{ title?: string, tables: [{ id: string, name: string, columns: [{ id: string, name: string }], rows: [{ id: string, cells: { [columnId]: { value: string } } }] }] }",
      "rule": "For get_data_schema/get_data_lake_schema, transform each database table into a structured_data table. Use columns: name, data_type, pk, nullable, description. Each database column becomes a row. Boolean fields become 'Yes' or empty string. For manage_data_sources, support every action response: dataSources list rows; a singular dataSource row for create/update; imported tables as child rows when tables is present; deletedId as a deletion-result row; rawSource as a row with dataSourceId and a safely truncated preview plus length metadata. For manage_data_tables, support tables list/import rows; a singular table row for rename; deleted/cascade results as a deletion-result row with cascadedCounts columns; impact as a table-impact row with count columns. Always include errors/warnings in a notes column. Push read-only results with push_content. For destructive delete results, only display after the caller has already completed any needed MCPViews review."
    },
    {
      "name": "structured_data",
      "description": "Tabular data with hierarchical rows, change tracking, sort/filter, and review mode",
      "scope": "tool",
      "tools": [
        "manage_data_draft"
      ],
      "data_hint": "{ title?: string, tables: [{ id: string, name: string, columns: [{ id: string, name: string, change?: 'add'|'delete' }], rows: [{ id: string, cells: { [columnId]: { value: string, change?: 'add'|'delete'|'update' } } }] }] }",
      "rule": "Transform draft changes into structured_data. Group changes by table_name into separate tables. Rows = database columns with changes. Columns = metadata field names. Set cell change markers: 'update' for modified values, 'add' for new values, 'delete' for removals. Push with push_review for accept/reject UI."
    },
    {
      "name": "rich_content",
      "description": "Markdown display with plugin citations for dependencies",
      "scope": "tool",
      "tools": [
        "get_dependencies"
      ],
      "rule": "Transform dependencies into rich_content markdown. Title: 'Dependencies'. Body: Group by source file. For each dependency, show source \u2192 target with import type and imported names. Add plugin citations for clickable source files."
    },
    {
      "name": "rich_content",
      "description": "Markdown display with plugin citations for file content",
      "scope": "tool",
      "tools": [
        "get_file_content"
      ],
      "rule": "Transform file content into rich_content markdown. Title: the file path. Body: Show file info (size, lines) then the content in a fenced code block with appropriate language detection from the file extension."
    },
    {
      "name": "rich_content",
      "description": "Markdown display for module overview",
      "scope": "tool",
      "tools": [
        "get_module_overview"
      ],
      "rule": "Transform get_module_overview output into rich_content markdown. Title: the directory path (monospace). If repository exists, note it. Summary line: N files, N exports, N internal deps, N external deps. Then sections: '## File Tree' as a code block listing paths with sizes. '## Exports' as a markdown table: | Name | Type | File | Signature |. '## Internal Dependencies' as rows: `source` \u2192 `target` (imported names). '## External Dependencies' as rows: `source` \u2192 `target`."
    },
    {
      "name": "rich_content",
      "description": "Markdown display for analysis stats",
      "scope": "tool",
      "tools": [
        "get_analysis_stats"
      ],
      "rule": "Transform get_analysis_stats output into rich_content markdown. Start with a summary line: '**N repositories** analyzed'. Then a metrics row as a markdown table: | Repositories | Code Units | Data Sources | Tables | Business Concepts | with totals as the single data row. Then an '## Analyzed Repositories' section with each repo as: **repo.name** (`language`) \u2014 N code units \u2014 last analyzed: date. If repo.fullName exists, show it in monospace below. If unanalyzed repos exist, add '## Unanalyzed' section listing their names."
    },
    {
      "name": "structured_data",
      "description": "Tabular data with hierarchical rows, change tracking, sort/filter, and review mode",
      "scope": "tool",
      "tools": [
        "get_business_concepts",
        "manage_knowledge_entries"
      ],
      "data_hint": "{ title?: string, tables: [{ id: string, name: string, columns: [{ id: string, name: string }], rows: [{ id: string, cells: { [columnId]: { value: string, change?: 'add'|'delete'|'update' } }, children?: [...] }] }] }",
      "rule": "Transform entries into structured_data with one table. Columns: name, description, mode, mappings. Root entries become rows; attributes become children (hierarchical rows). For mutations (create/update), set change: 'add' for new entries, 'update' for modified, null for reference. Mappings cell = comma-joined 'dataSource.table.column' strings. For read-only listing use push_content; for mutation review use push_review."
    },
    {
      "name": "rich_content",
      "description": "Markdown display with plugin citations for column context",
      "scope": "tool",
      "tools": [
        "get_column_context"
      ],
      "rule": "Transform column context into rich_content markdown. Title: 'Column: dataSource.table.column'. Body: Show column metadata (type, nullable, PK), description, linked business concepts, document links, and cross-column relationships. Add plugin citations for linked entities."
    },
    {
      "name": "ludflow_app",
      "description": "Embedded Ludflow web app, reusing the real Ludflow UI inside MCPViews",
      "scope": "universal",
      "rule": "Push directly to open the real Ludflow web app in an MCPViews iframe. The renderer creates a server-side one-time handoff from the active org-scoped MCP OAuth grant; it does not expose Better Auth session tokens to plugin JavaScript. Optionally pass organization_id and target_path. Supported initial iframe shell paths are / and /data-governance; Ludflow sanitizes unsupported paths to / before handoff.",
      "invoke_schema": "{ \"type\": \"object\", \"properties\": { \"organization_id\": { \"type\": \"string\", \"description\": \"Organization ID to open.\" }, \"target_path\": { \"type\": \"string\", \"description\": \"Relative Ludflow app shell path to open. Supported initial shell paths are / and /data-governance; unsupported paths are sanitized to /.\" } } }",
      "data_hint": "{ organization_id?: string, target_path?: string }",
      "standalone": true,
      "standalone_label": "Ludflow"
    },
    {
      "name": "ludflow_documents_home",
      "description": "Embedded Ludflow documents workspace using the real Ludflow web app UI",
      "scope": "universal",
      "rule": "Push directly to open Ludflow's real documents workspace inside MCPViews. The renderer creates a one-time app embed session through create_app_embed_session and opens / or /?documentId=... in an iframe.",
      "invoke_schema": "{ \"type\": \"object\", \"properties\": { \"organization_id\": { \"type\": \"string\", \"description\": \"Organization ID to open.\" }, \"document_id\": { \"type\": \"string\", \"description\": \"Document ID to open immediately.\" } } }",
      "data_hint": "{ organization_id?: string, document_id?: string }",
      "standalone": true,
      "standalone_label": "Documents"
    },
    {
      "name": "ludflow_data_governance",
      "description": "Embedded Ludflow data governance workspace using the real Ludflow web app UI",
      "scope": "universal",
      "rule": "Push directly to open Ludflow's real data governance workspace inside MCPViews. The renderer creates a one-time app embed session through create_app_embed_session and opens /data-governance with optional table, column, and Knowledge Dex focus params.",
      "invoke_schema": "{ \"type\": \"object\", \"properties\": { \"organization_id\": { \"type\": \"string\", \"description\": \"Organization ID to open.\" }, \"table_id\": { \"type\": \"string\", \"description\": \"Table ID to focus.\" }, \"column_id\": { \"type\": \"string\", \"description\": \"Column ID to focus.\" }, \"knowledge_open\": { \"type\": \"boolean\", \"description\": \"Open the Knowledge Dex panel immediately.\" }, \"knowledge_tab\": { \"type\": \"string\", \"enum\": [\"org\", \"personal\"], \"description\": \"Initial Knowledge Dex tab when the panel is open.\" } } }",
      "data_hint": "{ organization_id?: string, table_id?: string, column_id?: string, knowledge_open?: boolean, knowledge_tab?: 'org'|'personal' }",
      "standalone": true,
      "standalone_label": "Data Governance"
    },
    {
      "name": "ludflow_knowledge_dex",
      "description": "Legacy alias that opens the embedded Knowledge Dex view inside the Data Governance workspace",
      "scope": "universal",
      "rule": "Invoke the combined Data Governance workspace and open the embedded Knowledge Dex panel. Optionally pass organization_id and mode to control the initial tab.",
      "invoke_schema": "{ \"type\": \"object\", \"properties\": { \"organization_id\": { \"type\": \"string\", \"description\": \"Organization ID to open.\" }, \"mode\": { \"type\": \"string\", \"enum\": [\"org\", \"personal\"], \"description\": \"Initial Knowledge Dex tab to show.\" } } }",
      "data_hint": "{ organization_id?: string, mode?: 'org'|'personal' }",
      "standalone": false
    }
  ],
  "prompt_definitions": [
    {
      "name": "knowledge_dex_onboard",
      "description": "Guided onboarding to bootstrap your Knowledge Dex with business concepts, entities, and attributes. Interviews you about your domain, then creates entries with visual preview and approval.",
      "arguments": [],
      "source": "prompts/knowledge-dex-onboard.md"
    }
  ],
  "mcp": {
    "url": "https://app.ludflow.com/api/mcp",
    "auth": {
      "type": "oauth",
      "client_id": "fc99d041-1923-416c-8173-a4c02ae9b3a7",
      "auth_url": "https://app.ludflow.com/oauth/authorize",
      "token_url": "https://app.ludflow.com/oauth/token",
      "email_code_auth": {
        "enabled": true
      },
      "scopes": [
        "mcp:tools"
      ]
    },
    "tool_prefix": "ludflow__"
  }
}
# Unreleased

- Fix embedded Ludflow app origin selection so production MCPViews packages open the production app instead of the staging host.
- Let embedded app panels fit the MCPViews viewport without forcing an outer scroll area.

# Unreleased

- **feat**: Expose `manage_data_sources` and `manage_data_tables` in the Ludflow MCPViews plugin so agents can create data sources, import DDL/CSV tables, rename/delete tables, and inspect delete impact through data governance MCP tools.

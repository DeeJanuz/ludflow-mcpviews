# Unreleased

- Keep embedded Ludflow app renderers in the DOM iframe path even when MCPViews exposes native app panels, preserving Apps launcher stacking while retaining the one-time authenticated embed handoff.
